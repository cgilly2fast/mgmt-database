const MgmtOverviewMarkdown = `
 
## Mgmt-web :

- It has a front side for an admin.

- It uses Firebase and Reactjs.

- It uses node js version 16.

- It uses react-bootstrap and bootstrap for designing.

- It locally runs the same command as Ghotels-web.

- Build make the same command.

- Also, the deployment process is the same as Ghotels-web.

- This panel only uses by the admin.

- Admin can add new units and new Owners. Admin can show how many days a book is for a specific unit.

- Also, Admin creates an automated message on mgmt-web.

`;

export default MgmtOverviewMarkdown;
