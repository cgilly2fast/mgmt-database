const FunctionMarkdown = `

  ## Function :

  - Firstly, It has a back-end side for all panels, like ghotels-web, mgmt-web, and accounting.

  - Secondly, It uses Firebase Functions, Nodejs, and TypeScript.

  - It uses node js version 16.

  - It locally runs from the yarn serve command.

  - It is deployed from the yarn deploy command in the firebase.

  - To deploy the function on the development side, use the command

  ~~~js

  firebase use default

  ~~~

  - And To deploy the function on the production side, use the command 

  ~~~js

  firebase use prod

  ~~~

  - After deployment.
  `;

export default FunctionMarkdown;
